import '../css/secondpage.css';

export default function Navbar() {
    return (
        <header className={'page-2-header'}>
            <nav className={'page-2-nav'}>
                <img src="/images/react-logo.png" alt="React logo" />
                <span>ReactFacts</span>
            </nav>
        </header>
    )
}